# Runbook
## Run locally
Run `cargo run --manifest-path scaffold/Cargo.toml`; override `APP_BIND` or `SYNTHETIC_DATA` when needed.
## Network posture
No network is required; update reference bundles through an independently verified process.
