# Runbook
## Run locally
Run `cargo run --manifest-path scaffold/Cargo.toml`; override `APP_BIND` or `SYNTHETIC_DATA` when needed.
## Network posture
The scaffold makes no outbound calls. Enforce an OS firewall before non-synthetic evaluation.
