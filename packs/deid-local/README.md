# De-identification local
## What the app does today
Runs a deterministic identifier-marking preview on synthetic text without network calls.
## Owning it
The standalone Rust source, fixture, controls, and limitations ship in this folder.
