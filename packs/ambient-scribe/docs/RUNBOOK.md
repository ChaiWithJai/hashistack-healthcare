# Runbook
## Run locally
Run `cd app && cargo run`; set `APP_BIND` and `SYNTHETIC_DATA` as needed.
## Deploy targets
Requires approved voice, consent enforcement, encryption, and authenticated review.
