# Evidence Index

| Evidence | Status | Link | Reviewer note |
|---|---|---|---|
| decision | done | [docs/product-use-case.md](product-use-case.md), [RFC 0001](rfc/0001-clinician-platform.md) | Managed default (Lovable/Supabase glue) named; Rust boundary = gate engine + audit pipeline |
| service | done | `src/api.rs`, `/health`, [ci.yml](../.github/workflows/ci.yml) | Control plane serves the doctor UI and 15 API routes; README quickstart matches |
| contract | done | [tests/platform_contract.rs](../tests/platform_contract.rs) | Typed gate report; false-pass test: promote returns 409 naming the failing check, app stays sandboxed |
| reliability | done | `gate_blocks_promotion_until_fixed_then_admits_with_cosign`, `rollback_destroys_allocation_and_returns_to_synthetic_data` | Terminal states + rollback path exercised end-to-end through the public API |
| ops | done | [docs/ops-runbook.md](ops-runbook.md) | Full describe→audit workflow drivable from curl; smoke check documented |
| staging | done | [scripts/staging-up.sh](../scripts/staging-up.sh), [staging.yml](../.github/workflows/staging.yml) | Real Nomad dev agent + Vault dev server bootable on one machine; pressure test asserts job registration, transit round-trip, and stop-on-rollback (#2) |
| routing | done | [src/ladder.rs](../src/ladder.rs), [tests/ladder_contract.rs](../tests/ladder_contract.rs), [decision 0001](decisions/0001-agent-routing.md) | Verified escalation ladder rules→local→frontier; pack-declared policy; crash-visible operation rows; mock-tier tests only (decision 0002) (#4) |
| packs | in progress | [packs/post-op-monitor/](../packs/post-op-monitor/), `src/packs.rs`, `src/eject.rs` | Post-op-monitor converted to the full pack spec (#5): runnable axum scaffold (own CI job), synthetic seed, prompts/policies/gates/docs; ejection bundles ship its real source under `app/`. Remaining four packs still feature-string scaffolds |
| state | done | `src/store.rs`, [migrations/0001_init.sql](../migrations/0001_init.sql), [tests/store_contract.rs](../tests/store_contract.rs), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Postgres control store behind `CONTROL_DB_URL` (#7): DB-enforced `app_valid_state` transitions (trigger + composite FK) seeded from the same Rust const as the in-memory check, append-only state history + audit (triggers), Waypoint upsert-first operation rows written through, kill -9 restart-survival assertion in the staging pressure test; F4 resolved — model I/O off the platform lock with a concurrent-edit apply guard |
| audit | in progress | `src/audit.rs`, [tests/audit_broker_contract.rs](../tests/audit_broker_contract.rs), [decision 0004](decisions/0004-audit-broker-and-hmac.md), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Vault broker invariant (#8): probe-gated sinks (memory fallback + fsync'd JSONL file + control-DB), load-bearing operations 503-and-revert without ≥1 durable audit confirmation — kill-the-sink contract test holds the issue's bar; doctor free text salted-HMAC'd on every platform-wide surface, plaintext only on tenant-scoped views (app audit view, ejected COMPLIANCE.md); pressure test runs the file sink by default and asserts the HMAC boundary plus the promote row/HMAC'd prompt in Postgres. Remaining (issue stays open): object-storage archive sink, hipaa-core runtime-event ingestion (#5), export hash-chain digests |
| gates | in progress | `src/gates.rs`, [tests/evidence_contract.rs](../tests/evidence_contract.rs), [decision 0003](decisions/0003-evidence-basis-and-stubbed-verdicts.md) | Evidence-based verdicts over the post-op scaffold (#3): per-gate `basis` field, PHI markers with a stub-never-passes `stubbed` verdict, hosts checked against the pack's signed allowlist, adversarial broken-scaffold fixture failing every evidence gate; HIPAA citations (P1); attestation-time report frozen on promotion (F3); vault-stanza pressure assertion (F1). Remaining: real dependency scanner, observed sandbox egress, other packs as #5 converts them |
| vault | in progress | `src/hashi.rs`, `src/deploy.rs`, [scripts/staging-up.sh](../scripts/staging-up.sh), [vault/policies/tenant-app.hcl](../vault/policies/tenant-app.hcl), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Dynamic DB creds real in staging (#9): database engine wired to the staging Postgres (role tenant-app, 1h/2h TTL, scram host auth), promote issues + VERIFIES creds (SELECT 1 as the issued user) before recording the lease on the allocation, rollback revokes and PROVES it (login fails + role dropped from pg_roles; pressure test holds a sibling lease's password for the literal auth-then-fail); per-tenant ACL policy mounted at first promote and read back; Vault file audit device on from boot with the transit path asserted; rotate-proof (old ciphertext decrypts after key rotate); no-password-in-audit held by a cargo test. Remaining (issue stays open): policy-enforcing per-allocation tokens + Vault workload identity (Phase 1), per-tenant DB roles, consul-template resolution in a scheduled container, revoke-on-TTL-expiry observation (#6) |
| identity | in progress | [src/identity.rs](../src/identity.rs), [staging/identities.hcl](../staging/identities.hcl), [tests/identity_contract.rs](../tests/identity_contract.rs), [decision 0005](decisions/0005-operator-access.md), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Real principals on every /api route (#10): hcl-declared registry (unknown role fails load), bearer authn with audited dr-osei dev fallback vs strict 401s under IDENTITIES_FILE; tenancy 404s (existence undisclosed, `auth.cross_tenant_denied` on the owning tenant's stream), tenant-scoped lists, tenant-from-principal creation; staff role denied promote/co-sign + platform audit export (403, `auth.role_denied`) via one capability check; co-sign cryptographic — attestation binds principal id + sha256 of the frozen gate report (verified by test) + timestamp, typed cosigner reduced to a display-name check; SESSION_IDLE_SECS idle auto-logoff (the platform honoring its own gate) with `auth.session_expired`; pressure test drives two tenants + staff + a strict 1s-idle instance. Remaining (issue stays open): OIDC token source (model unchanged), NPI-verified clinician identity, operator Target/Session per decision 0005, UI login for strict mode |
| revision | pending | — | Awaiting first design-partner / reviewer feedback round |
| capstone | in progress | [docs/capstone-case-study.md](capstone-case-study.md) | Phase 0 slice done; Phase 1 (owned substrate) not started |
| public | pending | — | Publish after design-partner cohort; limitation to lead with: staging registers real Nomad jobs, but placement stays virtual (no containers run) |
