# Runbook
## Run locally
Run `cargo run --manifest-path scaffold/Cargo.toml`; override `APP_BIND` or `SYNTHETIC_DATA` when needed.
## Network posture
This preview performs no cloud call. A real sidecar must enforce destination allowlists and consent.
