# Evidence index

Each row states what was observed and what remains. A configuration file is
not observed proof. Dated Nomad and Vault results are historical integration
research. They do not describe the runtime selected in
[decision 0010](decisions/0010-minimum-lovable-runtime.md).

| Evidence | Status | Link | Reviewer note |
|---|---|---|---|
| decision | done | [product use case](product-use-case.md), [decision 0010](decisions/0010-minimum-lovable-runtime.md), [RFC 0001](rfc/0001-clinician-platform.md) | The minimum lovable runtime is Docker Compose on one host. Nomad and Vault are optional reference choices. Rust owns source and evidence decisions. |
| service | done | `src/api.rs`, `/health`, [ci.yml](../.github/workflows/ci.yml) | Control plane serves the doctor UI and 15 API routes; README quickstart matches |
| contract | done | [tests/platform_contract.rs](../tests/platform_contract.rs) | Typed gate report; false-pass test: promote returns 409 naming the failing check, app stays sandboxed |
| reliability | done | `gate_blocks_real_data_then_admits_disclosed_synthetic_demo`, `rollback_destroys_allocation_and_returns_to_synthetic_data` | Terminal states + rollback path exercised end-to-end through the public API |
| ops | done | [docs/ops-runbook.md](ops-runbook.md) | Full describe→audit workflow drivable from curl; smoke check documented |
| Historical Nomad and Vault integration | observed on 2026-07-12, not the selected runtime | [local infrastructure proof](evals/local-infrastructure-proof-2026-07-12.md) | The dated run observed Nomad, Vault, and Postgres behavior. Decision 0010 removed Nomad and Vault from the minimum lovable runtime. |
| DigitalOcean production proof | synthetic MVP proven | [exact-SHA proof](evals/digitalocean-production-proof-2026-07-12.md) | Exact source release, remote owned-export journey, measured latency, computed touch targets, and desktop/mobile screenshots; HTTP/IP-restricted and not approved for patient data |
| Hosted inventory and cost | observed on 2026-07-13 | [DigitalOcean inventory](evidence/minimum-lovable-inventory-2026-07-13.md) | One project Droplet costs $28.80 per month with weekly backups. The Gemma agent has token based usage cost. Teardown commands are recorded for both resources. |
| Local bounded generative fill | candidate proven | [Bonsai 4B experiment](investigations/0005-bounded-generative-fill.md) | A semantic mismatch was rejected; three distinct validated threshold fills compiled and passed 8/8 Rust tests in disposable exports |
| Retired Liquid/Hermes experiment | historical benchmark only | [local comparison](investigations/0006-liquid-hermes-coding-floor.md) | The experiment did not meet the trust boundary. Neither model is part of the product; Gemma is the only application model. |
| routing | done | [src/ladder.rs](../src/ladder.rs), [tests/ladder_contract.rs](../tests/ladder_contract.rs), [decision 0009](decisions/0009-agent-workspace-and-model-routing.md) | Production application edits use deterministic Rust rules; mock tiers test failure and concurrency only. Gemma is the sole application model and stays behind the bounded planning boundary. |
| packs | complete for local artifact goal | [`packs/`](../packs/), `src/packs.rs`, `src/eject.rs` | All 17 in-scope packs across web, stream, and local profiles ship standalone runnable Axum scaffolds, synthetic fixtures, and pack-owned artifact quality contracts; ejection bundles include real source under `app/` and CI tests every scaffold |
| state | done | `src/store.rs`, [migrations/0001_init.sql](../migrations/0001_init.sql), [tests/store_contract.rs](../tests/store_contract.rs), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Postgres persists apps, editable source workspaces, operations, and audit records. Real restart contracts recover a reviewable candidate and then its accepted checkpoint byte-for-byte. Boot revalidates source digests and candidate evidence. DB triggers still enforce legal app transitions and append-only history. |
| audit | in progress | `src/audit.rs`, [tests/audit_broker_contract.rs](../tests/audit_broker_contract.rs), [decision 0004](decisions/0004-audit-broker-and-hmac.md), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Vault broker invariant (#8): probe-gated sinks (memory fallback + fsync'd JSONL file + control-DB), load-bearing operations 503-and-revert without ≥1 durable audit confirmation — kill-the-sink contract test holds the issue's bar; doctor free text salted-HMAC'd on every platform-wide surface, plaintext only on tenant-scoped views (app audit view, ejected COMPLIANCE.md); pressure test runs the file sink by default and asserts the HMAC boundary plus the promote row/HMAC'd prompt in Postgres. Remaining (issue stays open): object-storage archive sink, hipaa-core runtime-event ingestion (#5), export hash-chain digests |
| gates | in progress | `src/gates.rs`, [tests/evidence_contract.rs](../tests/evidence_contract.rs), [decision 0003](decisions/0003-evidence-basis-and-stubbed-verdicts.md) | Evidence-based verdicts over the post-op scaffold (#3): per-gate `basis` field, PHI markers with a stub-never-passes `stubbed` verdict, hosts checked against the pack's signed allowlist, adversarial broken-scaffold fixture failing every evidence gate; HIPAA citations (P1); attestation-time report frozen on promotion (F3); vault-stanza pressure assertion (F1). Remaining: real dependency scanner, observed sandbox egress, other packs as #5 converts them |
| Vault reference research | observed locally, not the selected runtime | `src/hashi.rs`, `src/deploy.rs`, [historical staging script](../scripts/staging-up.sh), [historical policy](../vault/policies/tenant-app.hcl) | The earlier integration observed dynamic database credentials, transit rotation, policy reads, audit output, and lease revocation. The minimum lovable version does not run Vault. A later patient data architecture must select and prove a workload identity and key service. |
| identity | in progress | [src/identity.rs](../src/identity.rs), [staging/identities.hcl](../staging/identities.hcl), [tests/identity_contract.rs](../tests/identity_contract.rs), [decision 0005](decisions/0005-operator-access.md), [scripts/pressure-test.sh](../scripts/pressure-test.sh) | Real principals on every /api route (#10): hcl-declared registry (unknown role fails load), bearer authn with audited dr-osei dev fallback vs strict 401s under IDENTITIES_FILE; tenancy 404s (existence undisclosed, `auth.cross_tenant_denied` on the owning tenant's stream), tenant-scoped lists, tenant-from-principal creation; staff role denied promote/co-sign + platform audit export (403, `auth.role_denied`) via one capability check; co-sign cryptographic — attestation binds principal id + sha256 of the frozen gate report (verified by test) + timestamp, typed cosigner reduced to a display-name check; SESSION_IDLE_SECS idle auto-logoff (the platform honoring its own gate) with `auth.session_expired`; pressure test drives two tenants + staff + a strict 1s-idle instance. Remaining (issue stays open): OIDC token source (model unchanged), NPI-verified clinician identity, operator Target/Session per decision 0005, UI login for strict mode |
| evals | done | [evals/](../evals/), [scripts/evals.sh](../scripts/evals.sh), [docs/evals/scorecard.md](evals/scorecard.md), [evals.yml](../.github/workflows/evals.yml) | Two nested layers over 78 scenarios: layer 1 drives describe→eject over real HTTP for 70 pack/persona workflows plus edge, identity, and refusal cases; layer 2 unpacks, builds, boots, and browser-drives every owned artifact. Baseline: layer 1 458/458, layer 2 432/432; must_pass regressions exit nonzero |
| clinician UI | done for local goal | [web/index.html](../web/index.html), [desktop](evals/screenshots/platform-ui/catalogue-desktop.png), [mobile](evals/screenshots/platform-ui/catalogue-mobile.png), [built app](evals/screenshots/platform-ui/built-app.png) | Independently authored application shell with workspace summary, profile-aware 17-pack catalogue, selected-pack safeguards, responsive layout, builder/release/clinical/architecture views; real Playwright walkthrough completed with zero console errors |
| sampled artifact profiles | current, with human handoff pending | [12-app review](evals/sample-artifact-profiles.md), [structured scores](evals/sample-artifact-profiles.json), [reimport proof](../evals/reimport/proof.mjs) | All 12 sampled jobs passed. The Svelte and Rust source now run as one product. The post-op proof changes behavior, fixture, browser contract, and README together, runs the changed image, reimports the exact source without authority, and reexports it. A new person still needs to complete the README-only handoff. |
| Gemma signed recipe profile | proven on staging | [10-pack review](evals/gemma-signed-recipe-profile-2026-07-13.md), [machine results](evals/gemma-signed-recipe-profile-2026-07-13.json), [profiling script](../scripts/profile-gemma-planner.sh) | Gemma succeeded for 10 of 10 packs with no fallback. Median planning time changed from 36 seconds to 3.306 seconds. The first run did not preserve exact task text, so the latency change is directional evidence. |
| Accepted treatment preview | proven locally; CI gate added | [browser proof](../evals/treatment-preview/proof.mjs), [workflow](../.github/workflows/evals.yml), [checkpoint contract](../tests/platform_contract.rs) | Chromium runs all three signed recipes before export. Preview actions make no request and change neither checkpoint nor audit; rejected intent cannot replace accepted bytes. Studio and exported Svelte honor the same task-first/context-first choice. |
| merge governance | active | [standard](process/merge-standard.md), [doctor jobs](research/anthony-doctor-jobs.md), [merge gate](../scripts/merge-gate.sh) | Release candidates require exact-tree CI, provider proof, secret scanning, reversible backup, and human review before main changes |
| refusals | done (Phase 0) | [src/refusals.rs](../src/refusals.rs), [docs/investigations/0001](investigations/0001-enable-all-use-cases.md) | GOAL bar 7: the four RFC out-of-scope classes (9 enterprise outcomes, 10 ONC interop, 15 triage, 21 FDA device) answer 422 at describe with a written reason quoting the RFC rationale + `app.refused` audit event, nothing scaffolded. Keyword rules tuned against the whole eval corpus (unit tests run every scenario prompt both ways); a model-based screen slots behind the same seam (#12) |
| revision | pending | — | Awaiting first design-partner / reviewer feedback round |
| capstone | in progress | [docs/capstone-case-study.md](capstone-case-study.md) | Phase 0 slice done; Phase 1 (owned substrate) not started |
| public | pending | — | Publish after the doctor workflow, hosted verifier, exact export, and README only handoff are observed. Lead with the synthetic data and single host limits. |
